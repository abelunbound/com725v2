{
  "version": 2,
  "builds": [
    { "src": "app/**/*.py", "use": "@vercel/python" },
    { "src": "static/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/app/(.*)", "dest": "/app/$1" },
    { "src": "/static/(.*)", "dest": "/static/$1" }
  ]
}
